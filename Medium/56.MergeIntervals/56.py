class Solution:
    def merge(self, intervals: list[list[int]]) -> list[list[int]]:
        first_of_interval=None
        last_of_interval=None

        new_lst=[]

        for inte, i in enumerate (intervals):
            if inte[1]


        