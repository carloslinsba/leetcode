class Solution:
    ncandidates =None
    def isSolution(self, a:list[int], k:int):
        return True if k == len(n) else False
        

    def process_solution(self, a:list[int]):
        for item in a:
            if item==1:
                print(item)

    def construct_candidates(a:list[int], n:int, c:list): 
        c[0]=1
        c[1]=0 

    def backtrack(a:list[int],k:int , input:int):
        input = len(a)
        if self.isSolution(a,k):
            self.process_solution(a)

        pass



    def permuteHelper(self,nums : list[int], n)-> list[list[int]]:
        if len(n) == len(nums) -1:

    
    def permute(self, nums: list[int]) -> list[list[int]]:
        lenght = len(nums)
        self.ncandidates=2
        backtrack(nums,n)

