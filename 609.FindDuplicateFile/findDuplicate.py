import re

class Solution:


    # make a list for each path  
        #then  we need to separate the file path and the content
        #we can use regular expressions for it, but fo the sake of the example, a split should work
    #then we need to check if the content (i) is in the content list
        #
    # if we make 2 lists path() - content(), then the item position is the same
    # the easiest, and maybe most ineficcient way would be to have all as different lists and iterate througt then

    def findDuplicate(self, paths: List[str]) -> List[List[str]]:
            listOfPaths =[]
            listOfItemsPerPath =[]
            

    

    